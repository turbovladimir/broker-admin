{% extends 'base.html.twig' %}
    {% block body %}
        <div id="spinner">
            <div class="cv-spinner">
                <span class="spinner"></span>
            </div>
        </div>
        <div class="container">
            <div class="row mt-5 mb-5 align-items-center justify-content-between">
                {% block header %}
                    <div class="col-4">
                        <img class="img-fluid" src="{{ asset('content/img/loan/zaymi.png') }}">
                    </div>
                    <div class="col-4">
                        <img class="img-fluid" src="{{ asset('content/img/loan/increase.png') }}">
                    </div>
                {% endblock %}
            </div>
        </div>
                {% block container %}
                {% endblock %}
                {% block push %}
                    <div class="toast-container position-fixed bottom-0 end-0 p-3">
                        <div class="toast align-items-center text-bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
                            <div class="d-flex">
                                <div class="toast-body">
                                    Вам уже <a class="toast-link" href="https://yandex.ru" target="_blank">одобрено</a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endblock %}
    {% endblock %}