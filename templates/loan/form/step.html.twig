<div id="{{ step.id }}" {{ step.is_hidden is defined ? 'style="display: none;"' : '' }}>
    {% block content %}{% endblock %}
    <div id="block_error" class="row row-cols-1 mt-4 align-items-center justify-content-center g-sm-3"></div>
    <div class="row row-cols-1 mt-4 align-items-center justify-content-center">
        <div class="col-md-auto">
            <button id="{{ button.id }}" type="button" class="loan-btn gradient-green">{{ button.text }}</button>
        </div>
    </div>
</div>