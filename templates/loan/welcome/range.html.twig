<div class="row row-cols-1 justify-content-center p-3">
    <div class="col">
        <div class="row row-cols-lg-2 row-cols-md-1 mt-2 align-items-center justify-content-center">
            <div class="col">
                <div class="row row-cols-1">
                    <div class="col"><h3>Взять первый займ</h3></div>
                    <div class="col"><h2 class="text-green">БЕЗ ПРОЦЕНТОВ</h2></div>
                    <div class="col range shadow mt-2">
                        <label for="range_amount" class="form-label">Вам нужно</label>
                        <div style="display: flex; gap: 4px; align-items: center;">
                            <span class="range_val">2000р</span>
                            <input type="range" value="2000" min="2000" max="100000" step="2000" class="form-range"
                                   id="range_amount" oninput="document.getElementById('range_amount_value').innerHTML = this.value">
                            <span class="range_val">100000р</span>
                        </div>
                        <div class="m-2">
                            <h5><span id="range_amount_value">2000</span>р</h5>
                        </div>
                    </div>
                    <div class="col mt-4 text-center"><a href="#" data-bs-toggle="modal" data-bs-target="#{{ modal_phone_verify_id }}" class="loan-btn gradient-green d-block m-2" type="button">Получить займ</a></div>
                    <div class="col p-4 text-center"><p class="loan-p">Одобрение 99%</p></div>
                </div>
            </div>
            <div class="col d-sm-none d-md-none d-lg-block">
                <img class="img-fluid" src="{{ asset('content/img/loan/welcome.svg') }}">
            </div>
        </div>
    </div>
</div>
<style>
    @media (min-width: 576px) {
        --range_font-size:  7vw;
        --range-label-font-size: 4vw;
    }
    @media (min-width: 768px) {
        --range_font-size:  2vw;
        --range-label-font-size: 2vw;
    }

    #range_amount_value {
        font-size: var(--range_font-size);
    }

    .range {
        background-color: #F6F6F6;
        padding: 2em;
        border-radius: var(--border-radius);
    }

    .range_val {
        color: #CDCDCD;
        font-family: Inter-300;
        font-size: var(--range_font-size);
    }

    .range label {
        font-family: Inter-600;
        font-size: var(--range-label-font-size) !important;
        color: black;
        padding-bottom: 1em;
    }

    .range input::-webkit-slider-thumb {
        background: var(--green);
        width: var(--range-thumb-size);
        height: var(--range-thumb-size);
        border-radius: var(--range-thumb-radius);
    }

    .range input::-moz-range-thumb {
        background: var(--green);
        width: var(--range-thumb-size);
        height: var(--range-thumb-size);
        border-radius: var(--range-thumb-radius);
    }

    .range_row input::-ms-thumb {
        background: var(--green);
        width: var(--range-thumb-size);
        height: var(--range-thumb-size);
        border-radius: var(--range-thumb-radius);
    }
    .range input::-webkit-slider-runnable-track {
       height: var(--range-track-height);
    }
    .range input::-moz-range-track {
        height: var(--range-track-height);

    }
    .range input::-ms-track {
        height: var(--range-track-height);
    }
</style>